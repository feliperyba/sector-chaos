export enum EntityType {
  PLAYER = 0,
  PROJECTILE = 1,
  POWERUP = 2,
  TRAP = 3,
  CHEST = 4,
  DESTRUCTIBLE = 5,
  EXIT_DOOR = 6,
  EXPLOSION = 7,
  WEAPON_PICKUP = 8,
}

export default EntityType;
