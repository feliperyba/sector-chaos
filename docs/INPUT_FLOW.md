# Input Flow (moved)

> Folded into the netcode architecture page in the 2026-08 docs overhaul — this 16-line summary mis-attributed the orchestration entry point and predated the InputOrchestrator split.

The verified input round trip (with diagram) lives in [`architecture/netcode.md`](architecture/netcode.md).
